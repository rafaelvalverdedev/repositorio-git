# Agora vamos solicitar uma string e o um numero inteiro como entrada.
# Depois teremos que retornar a string repetida o numero de vezes informado.

string = input("Entrada de dados: ")
numero = int(input("Entrada de dados: "))

txtstring = string+" "
resultado = txtstring * numero

print(resultado)
    